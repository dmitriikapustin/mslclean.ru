name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # Lint и проверка сборки
  build:
    name: 🔨 Build & Lint
    runs-on: ubuntu-latest
    
    steps:
      - name: 📂 Checkout
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci --legacy-peer-deps

      - name: 🔍 Lint
        run: npm run lint || true

      - name: 🔨 Build
        run: npm run build

  # Деплой на сервер
  deploy:
    name: 🚀 Deploy to Production
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    steps:
      - name: 🚀 Deploy to VPS
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: root
          key: ${{ secrets.PRIVATE_KEY }}
          port: 22
          script: |
            cd /var/www/mslclean.ru || exit 1
            git pull origin main
            npm ci --legacy-peer-deps
            npm run build
            pm2 restart mslclean || pm2 start npm --name "mslclean" -- start
            echo "✅ Deployed successfully"
